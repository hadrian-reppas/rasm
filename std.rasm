mod string {
	
}

mod io {
	putd(d);
	putdln(d);

	putc(c);
	putcln(c);

	puts(s);
	putsln(s);

	stdin();
}

mod mem {
	alloc(n);
	free(ptr);
}

mod intrinsics {
	putd, putdln
	putc, putcln
	puts, putsln

	stdin

	alloc, free
}
