let COUNT: i64 = 2 >> 1;
let HASH_MAP: HashMap<String, Vec<i64>> = null;

fn map(data: Vec<A>, f: fn(A) -> B) -> Vec<B> {}

fn filter_map(data: Vec<A>, f: fn(A) -> Option<B>) -> Vec<B> {}

fn increment(p: *i64) {
	*p += 1;
}

fn loop(start: i64, end) {
	for (let i: i64 = start; i < end; i += 1) {
		let x: i64 = i + end;
		let y = x - i;
	}
}

fn main(argc: i64, argv: **u8) -> int {
	let x = 0;
	let y = &x;
	*y += 8 >>> 2;
	x
}
